fuction reverseString(str) {
    var novaString = "";
    for(var i= str.length - 1; i> -1;
    novaString += str[i];
    }
    return novaString;
}



 isPalindrome(str) {
 rn reverseString(str) === str;
 }

 fuction is Palindromo(str) {
     if (str.legth <2)
        return true;
        if (str[0] === str[str.legth -1])
        return false;
        var novaStr ="";
        for (var i = 1; i < st.length -2;
            novaStr += str [i];
            return isPalindromo(novaStr);
 }